import React from 'react';
import Body from './Body';
import Header from './Header';

const Home = (props) => {
    return(
        <div className="home">
            <Header/>
            <Body/>
        </div>
    )
};

export default Home;